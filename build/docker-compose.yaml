version: "3"
services:
  llm-frontend:
    image: llm-frontend:stream
    ports:
      - 3000:3000
    volumes:
      - ../src:/llm_frontend/src
    command: ['npx', 'vite', 'serve', '--port', '3000']